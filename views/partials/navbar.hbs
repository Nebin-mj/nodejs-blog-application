<nav class="navbar navbar-expand-lg navbar-light bg-light shadow">
   <div class="container-fluid">
      <div class="d-flex">
         <button
         class="navbar-toggler"
         type="button"
         data-bs-toggle="collapse"
         data-bs-target="#navbarSupportedContent"
         aria-controls="navbarSupportedContent"
         aria-expanded="false"
         aria-label="Toggle navigation"
      >
         <span class="navbar-toggler-icon"></span>
      </button>
      <a class="navbar-brand ms-1" href="/"><h1>Blog</h1></a>
      </div>
      <div class="collapse navbar-collapse" id="navbarSupportedContent">
         <ul class="navbar-nav me-auto mb-2 mb-lg-0">
            <li class="nav-item">
               <a class="nav-link 
                  {{#if (eq title "Home")}}
                     active
                  {{/if}}" aria-current="page" href="/">Home</a>
            </li>
            {{#if isAuthenticated}}
               <li class="nav-item">
                  <a class="nav-link
                  {{#if (eq title "Dashboard")}}
                     active
                  {{/if}}
                  " href="/dashboard">Dashboard</a>
               </li>
            {{/if}}
            <li class="nav-item">
               <a class="nav-link
                  {{#if (eq title "Users")}}
                     active
                  {{/if}}" href="/users">Users</a>
            </li>
            {{#unless isAuthenticated}}
               <li class="nav-item">
                  <a class="nav-link
                     {{#if (eq title "Login")}}
                        active
                     {{/if}}" href="/login">Login</a>
               </li>
               <li class="nav-item">
                  <a class="nav-link
                     {{#if (eq title "Register")}}
                        active
                     {{/if}}" href="/register">Register</a>
               </li>
            {{/unless}}
            {{#if isAuthenticated}}
               <li class="nav-item">
                  <a class="nav-link" href="/logout">Logout</a>
               </li>
            {{/if}}
         </ul>
      </div>
         <ul class="navbar-nav flex-row">
            {{#if isAuthenticated}}
               <li class="nav-item me-3 me-lg-1">
                  <a class="nav-link d-sm-flex align-items-sm-center" href="/dashboard">
                     <img src="{{diceBear}}{{signedInUser.profilePicture}}.svg" class="rounded-circle shadow" height="40"
                        alt="Black and White Portrait of a Man" loading="lazy" />
                     <strong class="d-none d-sm-block ms-1">{{signedInUser.name}}</strong>
                  </a>
               </li>
            {{/if}}
         </ul>
   </div>
</nav>